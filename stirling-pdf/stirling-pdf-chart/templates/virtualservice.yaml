apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: {{ .Release.Name }}
  namespace: {{ .Release.Namespace }}
spec:
  hosts:
  - {{ .Values.ezua.virtualService.endpoint | quote }}
  gateways:
  - {{ .Values.ezua.virtualService.istioGateway }}
  tls:
  - match:
    - port: 443
      sniHosts:
      - {{ .Values.ezua.virtualService.endpoint }}
    route:
    - destination:
        host: {{ include "stirlingpdf.fullname" . }}.{{ .Release.Namespace }}.svc.cluster.local
        port:
          number: 8080
  http:
  - route:
    - destination:
        host: {{ include "stirlingpdf.fullname" . }}.{{ .Release.Namespace }}.svc.cluster.local
        port:
          number: 8080
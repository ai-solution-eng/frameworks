apiVersion: v1
kind: Service
metadata:
  name: {{ include "presenton.fullname" . }}
  labels:
    {{- include "presenton.labels" . | nindent 4 }}
    app.kubernetes.io/component: presenton-server
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "presenton.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: presenton-server

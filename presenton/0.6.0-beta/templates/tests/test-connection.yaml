apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "presenton.fullname" . }}-test-connection"
  labels:
    {{- include "presenton.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "presenton.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

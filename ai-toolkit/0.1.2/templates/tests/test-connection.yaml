apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ai-toolkit.fullname" . }}-test-connection"
  labels:
    {{- include "ai-toolkit.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ai-toolkit.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

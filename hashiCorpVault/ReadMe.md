The Helm chart currentlly checked works on PCAI system.
In the configuration we have disabled TLS, thus if you look formward to enabling TLS you will have to explictly make changes to the values.yaml to enable it and define it dependecies.

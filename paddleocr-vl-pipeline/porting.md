# Porting Guide: PaddleOCR VL Pipeline on HPE AIE (PCAI)

## What This Is

A headless document OCR pipeline (PaddleOCR-VL + Gotenberg) deployed on HPE Private Cloud AI via BYOA (Bring Your Own Application). Upload a `.docx`, `.pptx`, or `.pdf` and get structured text back.

```
User → [Adapter :8080] → [Gotenberg :3000] → PDF → [Layout Detection (ONNX/CPU)] → [VLM on MLIS (GPU)] → Structured markdown
```

## Components

| Component | Where it runs | What it does |
|-----------|--------------|--------------|
| **PaddleOCR-VL (0.9B)** | MLIS (Tier 1, GPU) | Vision-language model for text/table/formula recognition |
| **Adapter** | Helm chart pod (Tier 2, CPU) | Orchestrates the pipeline, runs layout detection via ONNX |
| **Gotenberg** | Helm chart pod (Tier 2, CPU) | Converts docx/pptx to PDF via LibreOffice |

## Response Format

```json
{
  "filename": "report.docx",
  "pages": [
    {
      "page": 1,
      "blocks": [
        {"label": "title", "bbox": [x1,y1,x2,y2], "score": 0.95, "content": "Quarterly Report"}
      ]
    }
  ],
  "markdown": "# Quarterly Report\n\n...",
  "elapsed_seconds": 12.3
}
```

## Quick Test

**Note**: The data under /test is synthetic data randomly generated by LLM. Use with cautions.

```bash
$kubectl get pod -n paddleocr-vl-pipeline -l app.kubernetes.io/component=vl-api -o jsonpath='{.items[0].metadata.name}'
paddleocr-vl-pipeline-vl-api-5d6977cc77-8v627
$kubectl exec -it paddleocr-vl-pipeline-vl-api-5d6977cc77-8v627 -n paddleocr-vl-pipeline -- python3 -c "
import httpx
print('Downloading test.docx...')
doc = httpx.get('https://github.com/ai-solution-eng/frameworks/raw/main/paddleocr-vl-pipeline/test/test.docx', follow_redirects=True).content
print(f'Downloaded: {len(doc)} bytes')
print('Running OCR pipeline...')
resp = httpx.post('http://localhost:8080/ocr', files={'file': ('test.docx', doc)}, timeout=300)
print(f'Status: {resp.status_code}')
print(f'Response: {resp.text[:3000]}')
"
Downloading test.docx...
Downloaded: 37466 bytes
Running OCR pipeline...
Status: 200
Response: {"filename":"test.docx","pages":[{"page":1,"blocks":[{"label":"page","bbox":[0,0,1700,2200],"score":1.0,"content":"Quarterly Business Report - Q1 2026\nThis report summarizes the key performance indicators and strategic initiatives undertaken during the first quarter of 2026. All figures are in USD unless otherwise stated.\n1. Financial Summary\nTotal revenue for Q1 2026 reached $4.2M, representing a 15% increase over Q4 2025. Operating expenses remained stable at $2.8M, resulting in an operating margin of 33.3%.\nRevenue by Region\nRegion\nRevenue ($M)\nGrowth (%)\nNorth America\n2.1\n12\nEurope\n1.3\n18\nAsia Pacific\n0.8\n22\n2. Key Initiatives\nThe following initiatives were launched in Q1:\n· AI-powered document processing pipeline deployed on HPE Private Cloud AI\n· New partnership with PaddlePaddle for OCR capabilities\n· Gotenberg integration for automated document conversion\n3. Outlook\nQ2 2026 targets include expanding the AI pipeline to support 50+ document formats, achieving 99.5% OCR accuracy on mixed-language documents, and reducing processing latency to under 5 seconds per page. The formula for projected growth is:\nRevenue_Q2 = Revenue_Q1 × (1 + growth_rate) = $4.2M × 1.15 = $4.83M"}]}],"markdown":"\nQuarterly Business Report - Q1 2026\nThis report summarizes the key performance indicators and strategic initiatives undertaken during the first quarter of 2026. All figures are in USD unless otherwise stated.\n1. Financial Summary\nTotal revenue for Q1 2026 reached $4.2M, representing a 15% increase over Q4 2025. Operating expenses remained stable at $2.8M, resulting in an operating margin of 33.3%.\nRevenue by Region\nRegion\nRevenue ($M)\nGrowth (%)\nNorth America\n2.1\n12\nEurope\n1.3\n18\nAsia Pacific\n0.8\n22\n2. Key Initiatives\nThe following initiatives were launched in Q1:\n· AI-powered document processing pipeline deployed on HPE Private Cloud AI\n· New partnership with PaddlePaddle for OCR capabilities\n· Gotenberg integration for automated document conversion\n3. Outlook\nQ2 2026 targets include expanding the AI pipeline to support 50+ document formats, achieving 99.5% OCR accuracy on mixed-language documents, and reducing processing latency to under 5 seconds per page. The formula for projected growth is:\nRevenue_Q2 = Revenue_Q1 × (1 + growth_rate) = $4.2M × 1.15 = $4.83M\n","elapsed_seconds":2.39}
```

## Supported File Types

| Type | Flow |
|------|------|
| `.pdf` | → layout detection → VLM |
| `.png` `.jpg` `.jpeg` `.tiff` `.bmp` `.webp` | → layout detection → VLM |
| `.docx` `.doc` `.pptx` `.ppt` `.xlsx` `.xls` `.odt` `.odp` `.rtf` | → Gotenberg → PDF → layout detection → VLM |

## Key Configuration (values.yaml)

```yaml
vlApi:
  pipeline:
    vlmServerUrl: "<mlis-endpoint>/v1"
    vlmModelName: "PaddlePaddle/PaddleOCR-VL"
    layoutEnabled: true      # false to skip layout detection
    layoutDevice: "cpu"      # "cpu" or "cuda"
  mlisAuth:
    enabled: true
    apiKey: "<mlis-token>"
```

# MariaDB
## Testing
Tested with version 1.9.1 of AIE
## Additional Notes
Labels still need to be added to reflect Health State on AIE UI
To retrieve the mariadb password retrieve the base64 encoded password from the secret mariadb. Make sure to base64 decode it.